'use strict';

/**
 * Represents abstract strategy
 * @constructor
 * @param {string} config - config object
 */
function AbstractConfig(config) {
    this.config = config;
}

module.exports = AbstractConfig;
