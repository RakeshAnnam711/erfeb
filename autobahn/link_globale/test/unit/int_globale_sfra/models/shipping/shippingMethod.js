'use strict';

var assert = require('chai').assert;

describe('ShippingMethod', function () {
    it('can\'t be covered by unit test because of module.superModule usage', function () {
        assert.equal(true, true);
    });
});
