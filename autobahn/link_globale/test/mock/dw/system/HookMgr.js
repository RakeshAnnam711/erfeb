'use strict';

var HookMgr = {
    hasHook: function (hookName) { // eslint-disable-line no-unused-vars
        return true;
    },
    callHook: function (hookName) { // eslint-disable-line no-unused-vars
        return true;
    }
};

module.exports = HookMgr;
