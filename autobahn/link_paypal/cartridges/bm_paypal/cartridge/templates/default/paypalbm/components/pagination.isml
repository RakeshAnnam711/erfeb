<isif condition="${!pdict.pagingmodel.empty}">
    <isset name="pageURL" value="${pdict.pageurl}" scope="page"/>
    <isset name="pagination" value="${pdict.pagingModelParameters}" scope="page"/>

    <isinclude template="paypalbm/components/paginationForm" />
</isif>
