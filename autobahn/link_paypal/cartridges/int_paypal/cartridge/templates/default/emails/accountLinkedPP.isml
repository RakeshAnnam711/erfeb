<body>

    <h1>${Resource.msgf('email.account.greeting', 'registration', null, pdict.firstName, pdict.lastName)}</h1>

    <p>${Resource.msgf('email.account.linked.pp.text', 'notifications', null, pdict.currentSite, pdict.email)}</p>

</body>
