<div class="modal" id="removeProductModal" tabindex="-1" role="dialog" aria-labelledby="removeProductLineItemModal">
    <div class="modal-dialog" role="document">
        <div class="modal-content">
            <div class="modal-header delete-confirmation-header">
                <span class="modal-title" id="removeProductLineItemModal">${Resource.msg('heading.remove.product.confirmation.modal','cart',null)}</span>
                <button type="button" class="close" data-dismiss="modal" aria-label="Close"><span aria-hidden="true">&times;</span></button>
            </div>
            <div class="modal-body delete-confirmation-body">
                <p class="remove-cart-text">${Resource.msg('msg.remove.product.confirmation.modal','cart',null)}</p>
                <p class="product-to-remove"></p>
            </div>
            <div class="modal-footer delete-confirmation-footer">
                <button type="button" class="btn btn-outline-primary" data-dismiss="modal" aria-label="${Resource.msg('button.cancel','cart',null)}">${Resource.msg('button.cancel','cart',null)}</button>
                <button type="button" class="btn btn-primary cart-delete-confirmation-btn"
                        data-dismiss="modal" aria-label="${Resource.msg('button.confirm','cart',null)}">
                    ${Resource.msg('button.confirm','cart',null)}
                </button>
            </div>
        </div>
    </div>
</div>
