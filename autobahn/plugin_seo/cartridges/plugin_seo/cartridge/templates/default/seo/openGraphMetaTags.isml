<iscontent type="text/html" charset="UTF-8" compact="true" />

<isloop items="${pdict.ogTags}" var="ogTag" status="loopstatus">
    <meta property="${ogTag.ID}" content="${ogTag.content}" />
</isloop>
