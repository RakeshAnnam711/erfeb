<iscomment>
    This template gets overwritten in rvw_integrations_core.

    The reason for the duplication is because there are attribute level configurations that are integration specific

    Any change made here needs may also need to be reflected in the corresponding file in rvw_integrations_core
</iscomment>
<a class="line-item-name" title="${lineItem.productName}" href="${lineItem.productType === 'giftCertificate' ? URLUtils.url('GiftCertificate-Purchase') : URLUtils.url('Product-Show', 'pid', lineItem.id)}">${lineItem.productName}</a>

<isif condition="${!lineItem.isBonusProductLineItem}">
    <div class="remove-line-item">
        <button type="button" class="remove-btn remove-product icon-close" data-toggle="modal"
                data-target="#removeProductModal" data-pid="${lineItem.id}"
                data-name="${lineItem.productName}"
                data-action="${lineItem.productType === 'giftCertificate' ? pdict.actionUrls.removeGiftCertificateLineItem : pdict.actionUrls.removeProductLineItemUrl}"
                data-uuid="${lineItem.UUID}"
                aria-label="${Resource.msgf('text.remove.line.item','cart', null, lineItem.productName)}">
        </button>
    </div>
</isif>
