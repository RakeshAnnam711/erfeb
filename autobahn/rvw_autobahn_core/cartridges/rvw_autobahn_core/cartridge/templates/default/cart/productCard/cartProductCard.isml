<isinclude template="/components/modules" sf-toolkit="off" />

<isset name="hasEmbeddedBonusProducts" value="${lineItem.bonusProductLineItemUUID === 'bonus'}"  scope="page"/>
<isset name="bonusproductlineitem" value="${hasEmbeddedBonusProducts ? 'bonus-product-line-item' : ''}" scope="page"/>
<isset name="bonuslineitemrow" value="${hasEmbeddedBonusProducts ? 'bonus-line-item-row' : ''}" scope="page"/>

<div class="card" cypress-target="cartProductCard">
    <iscomment>
        WGACA MODIFICATION - custom style property
        <div class="card-body product-info ${bonusproductlineitem} uuid-${lineItem.UUID}">
    </iscomment>
    <div class="card-body product-info ${bonusproductlineitem} uuid-${lineItem.UUID}" style="min-height:240px">
        <!--- name and remove buttons --->
        <isif condition="${!lineItem.isBonusProductLineItem}">
            <isinclude template="integrations/cart/productCard/cartProductCard/deleteButton" />
        </isif>

        <div class="row ${bonuslineitemrow}">
            <div class="col-lg-6">
                <div class="row">
                    <div class="col-lg-12 d-flex flex-row">
                        <a class="item-image" href="${URLUtils.url('Product-Show', 'pid', lineItem.id)}">
                            <img class="product-image" src="${lineItem.images.small[0].url}" alt="${lineItem.images.small[0].alt}" title="${lineItem.images.small[0].title}" onerror="this.onerror=null;this.src='${URLUtils.staticURL('/images/noimage-small.png')}'" loading="lazy" width="150" height="200" />
                        </a>
                        <div class="item-attributes d-flex flex-column">
                            <isinclude template="cart/productCard/cartProductCardHeader" />
                            <isloop items="${lineItem.variationAttributes}" var="attribute">
                                <p class="line-item-attributes ${attribute.displayName}-${lineItem.UUID}">${attribute.displayName}: ${attribute.displayValue}</p>
                            </isloop>
                            <isloop items="${lineItem.options}" var="option">
                                <isif condition="${!!option}" >
                                    <div class="lineItem-options-values" data-option-id="${option.optionId}" data-value-id="${option.selectedValueId}">
                                        <p class="line-item-attributes">${option.displayName}</p>
                                    </div>
                                </isif>
                            </isloop>

                            <isinclude template="cart/productCard/cartProductCardAvailability" />

                            <isif condition="${lineItem.itemType === 'digital'}">
                                <div class="digital-product-delivery">
                                    <p><span class="delivery-type-label">${Resource.msg('label.deliveryType','product',null)}:</span> <span class="digital-delivery-label">${Resource.msg('label.digitalDelivery','product',null)}</span></p>
                                </div>
                            </isif>

                            <isif condition="${!lineItem.isBonusProductLineItem}">
                                <isinclude template="cart/productCard/cartProductCardEdit" />
                            </isif>
                        </div>
                    </div>
                    <isinclude template="integrations/cart/productCard/cartProductCard/additionalInfo" />
                </div>
            </div>

            <isif condition="${!lineItem.isBonusProductLineItem}">
                <div class="col-12 d-lg-none"><hr class="line-item-divider"></div>
            </isif>

            <isinclude template="cart/productCard/cartProductCardProductPrice" />

        </div>

        <isif condition="${lineItem.bonusProductLineItemUUID === 'bonus'}">
            <isinclude template="cart/productCard/cartEmbeddedBonusProducts" />
        </isif>

        <isinclude template="integrations/cart/productCard/cartProductCardIntegrationsBottom.isml" />
    </div>
</div>
